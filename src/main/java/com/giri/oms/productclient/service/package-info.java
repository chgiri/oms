/**
 * The product-client module's public API (ProductClient). `service.impl`
 * stays internal — same convention as every other module here.
 */
@org.springframework.modulith.NamedInterface
package com.giri.oms.productclient.service;
