/**
 * The customer-client module's public API (CustomerClient). `service.impl`
 * stays internal — same convention as every other module here.
 */
@org.springframework.modulith.NamedInterface
package com.giri.oms.customerclient.service;
