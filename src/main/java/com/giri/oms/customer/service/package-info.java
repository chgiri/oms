/**
 * The customer module's public API. Note: this covers only the `service`
 * package itself (the CustomerService interface) — `service.impl` is a
 * distinct, nested package and stays internal, so other modules can depend
 * on the interface but never the implementation.
 */
@org.springframework.modulith.NamedInterface
package com.giri.oms.customer.service;
