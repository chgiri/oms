/**
 * The product module's public API (ProductService). `service.impl` stays
 * internal — see the identical note on customer.service's package-info.
 */
@org.springframework.modulith.NamedInterface
package com.giri.oms.product.service;
