/**
 * Part of the auth module's public API — GlobalExceptionHandler maps these to
 * HTTP responses from the common module. Everything else in auth (entity,
 * repository, service.impl, mapper) stays internal.
 */
@org.springframework.modulith.NamedInterface
package com.giri.oms.auth.exception;
